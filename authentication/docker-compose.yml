version: '3.8'
services:
  mongodb:
    image: mongo:latest
    container_name: mongo
    ports:
      - "27017:27017"
  auth-service:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - mongodb
